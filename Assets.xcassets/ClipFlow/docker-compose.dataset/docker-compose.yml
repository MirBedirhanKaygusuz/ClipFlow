services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    volumes:
      - clipflow-storage:/tmp/clipflow
    restart: unless-stopped

volumes:
  clipflow-storage:
